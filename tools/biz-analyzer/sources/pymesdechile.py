# pymesdechile.cl is an abandoned directory — category pages are 72KB of template HTML
# with zero emails and no accessible company data. Last economic data shown is 2020.
# Stub only; yields nothing.

def scrape(sector: str, city: str | None = None):
    print("  [pymesdechile] skipped — site is defunct, no accessible data", flush=True)
    return
    yield  # make it a generator
